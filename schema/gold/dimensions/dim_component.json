{
  "table": "dim_component",
  "layer": "gold",
  "grain": "one row per component",
  "source": "Derived from silver telemetry data, deduplicated by component_id",
  "columns": [
    { "name": "component_id", "type": "string", "key": "PK (natural key)", "note": "e.g. COMP-0101A" },
    { "name": "hvac_machine_id", "type": "string", "key": "FK -> dim_hvac_machine" },
    { "name": "component_type", "type": "string", "note": "compressor | condenser | evaporator | expansion_valve" }
  ]
}